          ---- 语义分析之一

      内容提要:

6.1   符号表的地位和作用
6.2   符号表的组织与管理
6.3   符号表的结构设计
6.4   符号表的构造过程示例
6.5   运行时刻存储分配
                       标识符
  符号表是标识符的动态语义词典，属于
                       四种语
编译中语义分析的知识库；主要内容：
                       义信息
  ⑴ 名字 — 标识符源码，用作查询关键字；
  ⑵ 类型 -- 该标识符的数据类型及其相关信息；
  ⑶ 种类 -- 该标识符在源程序中的语义角色；
  ⑷ 地址 -- 与值单元相关的一些信息；

      ① 定义和重定义检查；
符号表
的功能   ② 类型匹配校验；
      ③ 数据的越界和溢出检查；
      ④ 值单元存储分配信息；
      ⑤ 函数、过程的参数传递与校验；…
6.2.1   符号表的工作原理

       ⑴ 遇 定义性标识符(在说明中语句)---顺着
    TOKEN的指针，把语义信息填入表中：
           （i , )   该标识符符号表项
       ⑵ 遇 应用性标识符(在操作语句中)---顺着
    TOKEN的指针，查符号表的相应项：
           （i , )   该标识符符号表项

6.2.2   符号表的查询、访问方式

  线性表、顺序表、索引表和散列表，皆可以采用。
6.2.3   符号表的维护、管理方式
   ※一个源文件有若干个函数组成，通常，每个函数对
  应一个符号表，此外，还是有一个公用符号表；

  ※符号表如何管理？往往取决于所属语言的程序结
 构，就 C语言来说，可以在内存设置一定长度的符号
 表区，并建立适当的索引机制，访问相应的符号表：
索引
机制         公用符号表       全局
                           符号表区
            现行函数符号表
   …            …           局部
          FUNCTION 2 符号表   符号表区
          FUNCTION 1 符号表
【例6.1】有下列函数过程：

FUNCTION exp(x:REAL;VAR y:INTEGER):REAL;
      CONST pai=3.14;
      TYPE arr=ARRAY[1..5,1..10] OF INTEGER;
      VAR a:arr; b,a:real;
      BEGIN … ; a[2,5]:=100; b:=z+6;… END;
⑴ 需要进符号表的标识符：
 exp(函数,附带信息:类型、参数情况和入口地址…),
 pai(常量),arr(类型),a(下标变量)，b(简单变量)，…
⑵ 怎样检查出：a 重定义、z 无定义以及下标变量
   a[2,5]的值地址在何处？…
  由于标识符的种类不同，导致语义属性也不尽相同；
怎样组织符号表？下面提供一个符号表的体系结构：

                                   名字 类型 种类 地址
             SYNBL（符号表）
token   NAME   TYPE   CAT ADDR
i ·      …                           PFINFL(函数表)

TYPEL（类型表）                           CONSL(常量表)
 TVAL TPOINT·         AINFL(数组表)
                                     LENL(长度表)
                      RINFL(结构表)
                                     VALL(活动纪录)
          …
6.3.1   符号表总表(SYNBL)
    ※ 结构：
             NAME   TYP   CAT   ADDR

    •NEME(名字)— 标识符源码(或内部码)
    •TYP(类型) – 指针，指向类型表相应项；
    •CAT(种类) – 种类编码：
        f(函数)，c(常量)，t(类型)，d(域名)，
        v,vn,vf(变量，换名形参，赋值形参)；
    •ADDR(地址) – 指针，根据标识符的种类不同，分
    别指向：PFINFL,CONSL,LENL,VALL,…
6.3.2   类型表(TAPEL)
  ※ 结构：
             TVAL    TPOINT


   •TVAL(类码)– 类型代码：
        i(整型)，r(实型)，c(字符型)，b(布尔型)，
        a(数组型)，d(结构型)，…
   •TPOINT(指针) – 根据数据类型不同，指向不同的信
   息表项：
        ① 基本数据类型(i,r,c,b)– nul(空指针)；
        ② 数组类型(a) – 指向数组表；
        ③ 结构类型(d) – 指向结构表;…
6.3.3   数组表(AINFL)
                             每维占表中一个纪录
   ※ 结构：

           LOW   UP   CTP   CLEN


    • LOW(数组的下界)--（C语言自动设为：0）;
    • UP(数组的上界)—
    • CTP(成分类型指针) – 指针，指向该维数组成分类
    型(在类型表中的信息);
    • CLEN(成分类型的长度)– 成分类型的数据所占值单
    元的个数；
        ※ 这里假定：值单元个数依字长为单位计算。
6.3.4   结构表(RINFL)            每个域占表中一个纪录
   ※ 结构：

              ID        OFF       TP

    • ID(结构的域名)—
    • OFF(区距)—是idk的值单元首址相对于所在记录值区
    区头位置；
        约定：off1=0，
            off2= off1+LEN(tp1), ……
            offn= offn-1+LEN(tpn-1)。
                              idn-1的长度
    • TP(域成分类型指针) – 指针，指向idk域成分类型
    (在类型表中的信息);
6.3.5   函数表(PFINFL)
                 ---- 过程或函数语义信息
  ※ 结构：

         LEVEL   OFF   FN   ENTRY PARAM …


    • LEVEL(层次号) –该过函静态层次嵌套号,
    • OFF(区距) –该过函自身数据区起始单元相对该
    过函值区区头位置 ；
    • FN(参数个数) – 该过函的形式参数的个数；
    • PARAM(参数表) – 指针，指向形参表；
    • ENTRY(入口地址) – 该函数目标程序首地址(运
    行时填写)；
6.3.6    其他表(…)

   ⑴ 常量表(CONSL)-- 存放相应常量的初值；
        ※ 结构：


   ⑵ 长度表(LENL) – 存放相应数据类型所占值单元个
   数；
      ※ 结构：


    ⑶ 活动纪录表(VALL) – 一个函数(或过程)虚拟的
    值单元存储分配表；是函数(或过程)所定义的数据，
    在运行时刻的内存存储映像，故称活动纪录。
     ※ 结构：
                …
 SYNBL                   PFINFL
 exp rtp f                ?         2   ENT   …
   x rtp vf       v2   CONSL         x rtp vf v2
   y itp vn       v3   3.14          y itp vn v3
 pai rtp c                            VALL
                       LENL
 arr      t                          临时变量值区
                        50
   a      v       v4              v5       b值
   b rtp v        v5              v4    数组a值区
 TYPEL                                   链接表
               AINFL              v3       y值
c,i,r,b       1 5         10      v2       x值
      a                           v1     exp值
      a       1    10 itp 1             管理区
【例6.2】有类型说明:
  TYPE arr = ARRAY [1..10] OF ARRAY [1..5] OF INTEGER;
  试填写符号表。
                             SYNBL
     TYPEL                    arr          t

       i
       r
                                                     LENL
       c                                             200

       b                      AINFL
       a                       1      10               20

       a                       1      5        itp     4



 设：实型占8个存储单元，整型占4个单元，布尔型和字符型占1个单元。
【例6.3】有类型说明: TYPE rec = RECORD
                            u: INTEGER;
                            v: ARRAY [1..10] OF BOOLEAN;
     试填写符号表。                r: RECORD x, y : REAL END
     SYNBL                END;
      rec             t    LENL         RINFL
       u        itp   d      30         u          0       itp
       v              d      4          v          4
       r              d      10          r         14
       x        rtp   d      16          x         0       rtp
       y        rtp   d      8           y         8       rtp

           TYPEL             8

       i,r,c,b
            d                       AINFL
            a                 1    10        btp       1

            d

    设：实型占8个存储单元，整型占4个单元，布尔型和字符型占1个单元。
【例6.4】 有过程说明:
           PROCEDURE P1(VAR x: REAL; y: INTEGER);                        1
             ……                                                       P1 2
            BEGIN …… END;
    设P1所在层LEVEL=1，即所定义的层LEVEL=2, 试填写符号表。
      SYNBL                              PFINFL
      P1              p
                                 2         ?         2    Entry
      x         rtp   vn   ?
      y        itp    vf   ?

                                     x         rtp       vn       ?
      TYPEL
                                     y         itp       vf       ?
           i
           r               注： ？—— 该标识符的值单元首址，
           c                             为相对地址（LEVEL, offset）
           b                   LEVEL ——该标识符所在层次号，
                               offset ——区距，存储分配时可定。
   设：实型占8个存储单元，整型占4个单元，布尔型和字符型占1个单元。
 ※解决的问题：标识符变量的地址分配与对它们的访问。
6.5.1 标识符值单元分配
 值单元分配分两类：
 1.静态分配
    在编译阶段即可完成真实的地址分配。在编译时
  对所有数据对象分配固定的存储单元，且在运行时始
  终保持不变。

 2.动态分配
    指在运行时刻进行的值单元分配，在编译时只能进
 行相对地址分配。
    ·栈式动态分配；
    ·堆式动态分配。
 注：值单元分配是以过程函数为单位的。
6.5.2 活动记录
 1.三个概念

   过程：一个可执行模块，过程或函数，通常完成特
      定的功能。
   活动：过函的一次执行。每执行一次过程函数体，
      则产生该过函的一个活动。
   活动记录：一个有结构的连续存储块。用来存储过
        函一次执行中所需要的信息。

    活动记录仅是一种存储映像，编译程序所进行
  的运行时刻存储分配是在符号表中进行的。

    如果不支持可变数据结构，活动记录的体积是
  可以在编译时确定的。
6.5.2 活动记录（续）
 2.活动记录的结构                  VALL




                                   局部数据
（1）连接数据区              TOP   临时单元
 ·返回地址：断点地址；                内情向量

 ·动态链：                      局部变量

    指向调用该过程的主调程序的           形式单元




                                   连接数据
  活动记录的指针；                  静态链
 ·静态链：                      动态链
  指向静态直接外层活动记录的指针。    SP    返回地址

（2）形式单元              （4）栈指针
   用来存放实参的值或地址。       SP — 指向现行过程活动记
                      录的起点，即第一个单元；
（3）局部数据区
                      TOP —指向（已占用）栈顶
    用来存放局部变量、内情向量、    单元，即活动记录的最后一
  临时单元。               个单元。
6.5.3 简单的栈式存储分配
·以C语言为例: 没有分程序结构，过程定义不允许嵌套，
         但允许过程的递归调用。
1．C语言程序的存储组织            TOP
                                    R的活动记录
                         SP
【例6.5】 C语言过程调用关系：                   Q的活动记录
  Main( ) Q( ) R( )              Main的活动记录

  则，活动记录栈状态为：                       全局数据区
                          TOP
                                     临时单元
2．C的活动记录                             内情向量
  其中：                                局部变量
  Old SP值，即前一活动记录的首地址；               形式单元
                                     参数个数
                                     返回地址

                              SP      Old SP
6.5.3 简单的栈式存储分配（续）                             ……
3．C语言的过程调用与返回                          形参区      …      子过程
（1）过程调用                                         t1     P的活
                                              参数个数     动记录
   ① 过程调用的四元式序列：                              返回地址
     (param, entry(t1), _, _)                 Old SP
              ……                        TOP            主调过程
 第n个实参地址
                            _)
     (param, entry(tn), _, 过程P的入口地址
                                              ……       活动记录
                                         SP
     (call, entry(P), n, _)
                             参数个数       TOP
   ② 对应的目标指令：                                  ……
     ·(param, entry(ti), _, _)对应的指令：           …
                                                        子过程
        (i+3)[TOP] := entry(ti).Addr            t1      P的活
                                         L
   ③ 子过程P需完成的工作：定义自                             n       动记录
        //将ti地址填到活动记录的形参区去
   己的活动记录；
    ·(call, entry(P), n, _)对应的指令：             返回地址
     SP :=  TOP+1:= SP
         1[TOP]           //定义过程P的SP
                         //保护现行SP               SP
                                         SP
                                        TOP
     1[SP]  := 返回地址
         3[TOP]  := n     //保护返回地址
                         //传递参数个数              ……      主调过程
          := TOP+L
     TOP JSP P           //定义新TOP                      活动记录
                                        SP
6.5.3 简单的栈式存储分配（续）
3．C语言的过程调用与返回
（2）过程返回
  ① 过程返回的四元式：
   (ret, _, _, _)
                         TOP
                                ……
  ② 对应的目标指令：                     …
                                       子过程
                           L     t1    P的活
  TOP := SP-1  //恢复TOP
  SP := 0[SP]  //恢复SP            n     动记录

  X := 2[TOP]                   返回地址

     //取返回地址，X为某一变址器             SP
                          SP
                         TOP    ……     主调过程
  UJ 0[X]
                                       活动记录
     //按X中的返回地址实行变址转移      SP

                                  X
                                返回地址
6.5.4 嵌套过程语言的栈式存储分配
1.标识符的作用域
·过程嵌套的一个关键问题：标识符的作用域问题 。

   标识符的作用范围往往与它所处的过程相关，也就
 是说，同一个标识符，在不同的程序段里，代表不同的
 对象，具有不同的性质，因此要分配不同的存储空间。

 ·标识符的有效范围： 服从最小作用域原理；

 （1）在外层未定义，而在内层定义的，服从内层定义；
 （2）在外层已定义，而在内层未定义的，服从全范围；
 （3）在外层已定义，而在内层也定义的，在外层服从
 外层定义，在内层服从内层定义。
6.5.4 嵌套过程语言的栈式存储分配（续）
2.活动记录
 ·问题的提出：
   过程Q可能会引用到它的任意外层过程的最新活动记
 录中的某些数据。
 ·解决问题的思想：
    为了在活动记录中查找这些   TOP   临时单元
 非局部名字所对应的存储空间，          内情向量
 过程Q运行时必须设法跟踪它的所         局部变量
 有外层过程的最新活动记录的地
                         形式单元
 址。
                         参数个数
 ·解决方案：




                                  连接数据
                         静态链
   活动记录中增加静态链！使          返回地址
 其指向直接外层的最新活动记录          Old SP
 的首地址；             SP
3.嵌套层次显示表(display)和活动记录结构
(1)连接数据区： 0～2；
  ·老SP — 主调过程的活动记录首址；
    ·全局display地址 — 主调过程的显示区表首址；
(2)参数个数： 3；                  TOP       ……
                                      临时单元
(3)形参值单元区： 入口为4；
                                       ……




                                                     用于访问外层的变量
 ·换名形参（vn）—                           内情向量
     分配2个单元（地址传递）；                     ……
 ·赋值形参（vf）— 按相应类型长度分配；                局部变量
(4)显示区表(display)： 占l+1个单元；             ……
                              l+1   显示区表(Display)
   l为层次号，包含直接外层嵌套的l个过
                                       ……
 程的活动记录的首址，再加上本过程的活
                                  4   形式单元
 动记录首址；
(5)局部变量区： 入口为off + l + 2；          3     参数个数




                                                     连接数据
 ·off为形参区最后一个值单元地址；                2   全局Display地址
 ·局部变量值单元按相应类型长度分配地址；              1     返回地址
  ·类型标识符、常量标识符等不分配值单元；                    Old SP
                                   0
 (6)临时变量区：                    SP
   编译系统定义的变量，按局部变量值单元分配原则分配地址；
4. Display表的建立
  设过程调用关系为Q( )  R( )，且R( )的层次号为l，
则Q与R的display表的关系如下:
    TOP               ……                       ……
                     临时单元                     临时单元
                      ……                       ……
                     内情向量                     内情向量
                      ……                       ……
                     局部变量                     局部变量
                      ……           拷贝l个单元      ……
  l+1个单元
                   显示区表(Display)            显示区表(Display)
                      ……                       ……
                     形式单元                     形式单元
           拷贝自身的




                      参数个数                     参数个数
                   全局Display地址              全局Display地址
                      返回地址                     返回地址
          SP




                       Old SP                   Old SP
     SP
                    R的活动记录                    Q的活动记录
【例6.6】 设有Pascal程序片段如下：                                        变量作用域：
层次：0     program P;
           var a, x: integer;
       1   procedure Q(b: integer);
             var i: integer;
         2   procedure R(u: integer; var v: integer);
                var c, d: integer;
                begin
                  if u=1 then u=u+1;                    u,v
                    ……                                  c,d
                  v:= (a+c)+(b-d);
                  ……                                          b,i
                end {R}
             begin
                ……
                R(1, x);
                ……                                                  a,x
             end {Q}
       1   procedure S;
             var c, i: integer;
             begin
                a:=1;                                         c,i
                Q(c);
                ……
             end {S}
           begin
             a:=0;
             S;          过程调用关系为：PSQR
             ……
           end.
  【例6.6】 试给出程序运行时的活动记录关系。

                                 u-(2,4)
          58-61          d                                      c-(1,6)
  R                              v-(2,8)
          54-57 局部变量 c          c-(2,15)         b-(1,4)       i-(1,10)
活动记录                                            i-(1,10)
             53      41         d-(2,19)
  Q          52         27
活动记录                                                              a-(0,5)
         51 Display表 0 37-40 局部变量 i
                                         23-26           i        x-(0,9)
  S   49-50           v   36          27
                             Display表    19-22 局部变量 c
活动记录 45-48 形式单元 u         35           0                   9-12           x
                                            18         13
  P      44 参数个数 2 31-34 形式单元 b                             5-8 局部变量 a
                                            17 Display表 0
                          30 参数个数 1                           4 Display表 0
活动记录     43 全局Display35                     16 参数个数 0
                          29 全局Display17                      3 参数个数 0
         42 返回地址                            15 全局Display 4
活动记录栈                     28 返回地址                             2 全局Display 0
         41 Old SP   27                     14 返回地址
                          27 Old SP   13                      1 返回地址
                                            13 Old SP    0
        R的活动记录(2层)                                            0 Old SP    0
                             Q的活动记录(1层)
                                       S的活动记录(1层)
                                                 P的活动记录(0层)
5.值单元的地址分配
  ·值单元分配是依据活动记录的结构，在符号表中进行的。
 【例6.7】设有Pascal程序片段如下，P1所在层level=2；
     PROCEDURE P1( x: REAL; VAR y: BOOLEAN );
         CONST pai=3.14;
         TYPE arr=ARRAY [1..10] OF INTEGER;
         VAR m: INTEGER;
                a: arr;
                l: REAL;
         FUNCTION F1( z: REAL; k: INTEGER ): REAL;
               BEGIN …… END;
         ……;
         BEGIN
               ……;
         END;
  试给出符号表组织及值单元分配情况。
设：(1)实型占8个存储单元，整型占4个单元，布尔型和字符型占1个单元。
  (2)换名形参vn分配2个单元，赋值形参vf按相应类型长度分配；
接上页：
过·程 P1
                      SYNBL                               PFINFL
                 P1            p              3       3    2 Entry
                  x  rtp vf (3,4)             4       3    2 Entry
                                                                                 P1的VALL
    定义的层次为 l=3




                  y btp vn (3,12)
                                                                         62-69            l
                 pai rtp c
                                                  x       rtp vf (3,4) 22-61              a
                 arr           t
                                                  y       btp vn (3,12) 18-21 局部变量        m
                  m     itp    v (3,18)                   rtp vf (4,4)
                                                  z                       17
                  a            v (3,22)
                                                  k       itp vf (4,12)   16
                  l     rtp v (3,62)
                                                                           15
                 F1     rtp f                     CONSL
                                                                           14 Display表
                  z     rtp vf (4,4)              3.14            LENL
                                                                         12-13            y
                  k     itp vf (4,12)                             40
                                                                          4-11 形式单元       x
                       TYPEL                                                3 参数个数        2
                                              AINFL                         2 全局Display
                  i,r,c,b
                                          1       10        itp      4      1 返回地址
                        a
                                                                            0 Old SP
【习题6.1】解释下述词语：
 ⑴ 符号表 ； ⑵ 标识符的语义信息；
 ⑶ 符号表的功能；（4）c语言符号表的管理方式。
【习题6.2】设有程序片断如下，试填写符号表：
    float exe(x,y)
        int x, y[5][10];
      { float a; int b[5][10];
           …
          b[2,5]=15; …
      }
  ※ 如何确定下表变量 b[2][5]的地址？
【习题6.3】P236_1,2,3
【习题6.4】P268_1,2
FUNCTION exp(x:REAL;VAR y:INTEGER):REAL;
      CONST pai=3.14;
      TYPE arr=ARRAY[1..5,1..10] OF INTEGER;
      VAR a:arr; b:real;
      BEGIN … ; a[2,5]:=100; b:=6;… END;
 ① exp , rtp , f ,          函数表
 ② x , rtp , vf ,           活动记录
 ③ y , itp , vn ,           活动记录
 ④ pai , rtp , c ,          常数表
 ⑤ arr , atp , t ,          长度表
 ⑥ a , atp , v ,            活动记录
 ⑦   b   , rtp , v   ,      活动记录
